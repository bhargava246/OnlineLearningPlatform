# Environment Variables for Local Development

# Required for Replit OAuth (set to your local domain for development)
REPLIT_DOMAINS=localhost:5000

# Required for local development (will be auto-set to skip OAuth)
REPL_ID=local-development-id

# Optional: Set custom session secret
SESSION_SECRET=your-secret-key-here

# Optional: Set custom JWT secret
JWT_SECRET=your-jwt-secret-here

# MongoDB Connection (already configured in server/config/database.js)
# No additional env vars needed for MongoDB

# NOTE: For local development, OAuth authentication will be skipped
# You can use the manual login/register forms on the auth page

# For production deployment on Replit, these are automatically set:
# REPL_ID=<auto-generated>
# REPL_OWNER=<your-username>
# REPL_SLUG=<your-repl-name>